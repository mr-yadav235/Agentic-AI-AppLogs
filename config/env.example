# Multi-Model AI Configuration
# =================================================

# OpenAI Configuration (Required for backward compatibility)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic Claude Configuration (Optional - enables Claude models)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google Gemini Configuration (Optional - enables Gemini models) 
GOOGLE_API_KEY=your_google_api_key_here

# Elasticsearch Configuration
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=your_elasticsearch_password

# Application Configuration
NODE_ENV=development
PORT=3000

# AI Model Preferences (Optional)
# DEFAULT_AI_PROVIDER=openai  # Options: openai, claude, gemini
# ENABLE_MODEL_FALLBACK=true  # Enable automatic fallback to other models on failure

# Performance Monitoring (Optional)
# LOG_AI_PERFORMANCE=true     # Log AI model performance metrics
# AI_RESPONSE_TIMEOUT=30000   # Timeout in milliseconds

# ===================================================
# Setup Instructions:
# 1. Copy this file to .env in your project root
# 2. Replace placeholder values with your actual API keys
# 3. At minimum, you need OPENAI_API_KEY for the system to work
# 4. Additional AI providers (Claude, Gemini) are optional but recommended
# 5. Restart your application after making changes
# ===================================================